# CorpBrain MVP 첫 슬라이스 — 의사결정 로그 · 루프 2 (조기 종료 체크포인트)

- 시작: 2026-08-12, W4/FR-005부터 재개
- 이전 루프 이력: `docs/loop/DECISION_LOG.md` (MINOR 10건 + 사용자 확정 U-001~U-003)
- 확정 사항(카운트 제외): `docs/decisions/implementation-decisions.md` 10건, 스펙 §4.4 출력 파일명 규칙

## 카운터
CORE: 0
MINOR: 8

<!-- 위 두 줄은 오케스트레이터만 갱신한다. 권위 있는 값은 아래 엔트리 줄 수:
     grep -c '^- \[CORE\]' docs/loop/DECISION_LOG_L2.md
     grep -c '^- \[MINOR\]' docs/loop/DECISION_LOG_L2.md
     조기 종료: CORE >= 3 또는 MINOR >= 10 -->

## 엔트리 (append only — 단일 `cat >>` 명령으로만 추가, 기존 줄 편집 금지)
- [MINOR] L2-001 | FR-011 | agent:orchestrator | 2026-08-12T23:10:00+09:00 | `## 태그·키워드` 섹션은 태그를 쉼표+공백으로 이어 한 줄로 렌더한다(`영업, 실적, 2026`). 해시태그(`#영업`)나 불릿 목록을 쓰지 않는다 | 근거: 스펙 §4.4가 `<태그 목록>`이라고만 표기해 형식 미지정. 한 줄 쉼표 구분이 마크다운 뷰어·grep 모두에서 단순하고, 빈 배열일 때도 헤더 아래 빈 줄만 남아 "섹션 누락 불가" 규칙을 자연스럽게 만족한다.
- [MINOR] L2-002 | FR-009 | agent:worker-ollama | 2026-08-12T23:35:00+09:00 | 헬스체크 URL은 `--ollama-url`을 항상 슬래시로 끝나는 베이스로 만든 뒤 `api/tags`를 결합한다 — 베이스에 경로가 있으면(`http://host/ollama`) 그 경로를 보존한다(`http://host/ollama/api/tags`) | 근거: 스펙 §4.3은 URL 조립 규칙 미지정. 맨 urljoin은 베이스의 마지막 경로 세그먼트를 잘라내 리버스 프록시 뒤의 Ollama를 못 찾는다. 기본값(`http://127.0.0.1:11434`)에서는 동작 차이가 없다.
- [MINOR] L2-003 | FR-010 | agent:orchestrator | 2026-08-12T23:55:00+09:00 | 요약 요청은 `POST /api/generate`에 `{"stream": false, "format": "json", "options": {"temperature": 0}}`로 보내고 타임아웃 기본 300초 | 근거: 스펙 §4.3은 "Ollama 로컬 HTTP로 고정 필드 JSON을 요청"만 정하고 엔드포인트·옵션·타임아웃은 미지정. `format=json`이 Ollama의 JSON 강제 모드라 파싱 실패율이 낮고, temperature 0으로 같은 문서에 대한 재현성을 높인다. 스트리밍은 관문이 단발 JSON 응답을 반환하는 계약과 맞지 않아 끈다.
- [MINOR] L2-004 | FR-005 | agent:worker-guard | 2026-08-12T05:05:00+09:00 | 스캔 단계의 `permission_denied` 판정은 지원 확장자 파일에 한해 `os.access(path, os.R_OK)` 프로브로 한다(파일을 열지 않는다). 판정 순서는 긴 경로 → 미지원 확장자 → 권한 프로브 | 근거: 스펙 §5는 "권한 거부는 스킵"만 정하고 탐지 수단 미지정. 스캐너는 파일을 열지 않는다는 FR-004 계약(테스트로 고정됨)상 open/stat으로는 읽기 거부를 감지할 수 없다. 미지원 파일은 어차피 처리 대상이 아니라 확장자 사유가 더 유용해 프로브보다 앞에 둔다.
- [MINOR] L2-005 | FR-005 | agent:worker-guard | 2026-08-12T05:05:00+09:00 | 디렉터리 접근 거부는 해당 **디렉터리 경로** 1건을 `permission_denied`(detail=`scanner.DIRECTORY_DENIED_DETAIL`)로 스킵 리포트에 남기고 하위는 순회하지 않는다. 단 입력 루트 자체의 접근 실패(없음·거부)는 스킵으로 기록하지 않는다 | 근거: 스펙 §5는 파일 단위 스킵만 열거하나, 통째로 못 읽은 디렉터리를 조용히 지나가면 사용자가 누락을 알 수 없다. 루트 실패는 스펙 §3-5의 선행 조건 실패(비-0 종료, FR-015·016 담당)이므로 부분 스킵으로 위장하면 안 된다.
- [MINOR] L2-006 | FR-005 | agent:worker-guard | 2026-08-12T05:05:00+09:00 | 상한 초과 시 `ScanFindings.skipped`는 그때까지 분류된 스킵 목록을 그대로 유지하고, `discovered_count`는 상한 초과 여부와 무관하게 항상 처리 대상 수로 채운다(미지원·스킵 파일은 상한 계산에 넣지 않는다) | 근거: 오케스트레이터가 스킵 목록 유지 여부를 구현자에게 위임. 종료 리포트(FR-016)가 "발견 N건 > 상한 M건" 문구와 스킵 사유를 한 번에 렌더할 수 있도록 두 필드를 항상 채우는 편이 어댑터 분기를 줄인다.
- [MINOR] L2-007 | FR-016 | agent:orchestrator | 2026-08-13T00:20:00+09:00 | 종료 코드: 정상·부분 실패=0, 선행 조건 실패(폴더 없음·Ollama 미탐지)=1, 상한 초과=3. argparse 사용법 오류는 표준 2를 유지 | 근거: 스펙 §5는 "부분 실패 0 / 선행 조건 실패 비-0 / 상한 초과 중단+알림"만 정하고 구체 코드는 미지정(implementation-decisions 미결정 1번). 상한 초과를 선행조건 실패(1)와 다른 코드(3)로 분리해 스크립트가 두 상황을 구분할 수 있게 하되, argparse 표준 2와 충돌하지 않게 3을 골랐다.
- [MINOR] L2-008 | FR-016 | agent:orchestrator | 2026-08-13T00:20:00+09:00 | 진행 로그 계층: 코어는 로깅하지 않고 `core/report.py`가 종료 요약을 문자열 리스트로 만들며, CLI가 그것을 stderr로 출력한다(코어에 logging 미도입) | 근거: 스펙 §4.1은 "진행 로그는 stderr"만 정함(implementation-decisions 미결정 2번). 코어가 순수 함수로 남아야 후속 UI 어댑터가 같은 report.py를 재사용해 화면에 표시할 수 있다(스펙 §4.5).

STOP REASON: ALL_WAVES_DONE
완료(루프 2): W4(FR-005·009) → FR-010 → FR-011 → FR-012·017 → FR-013·015 → FR-016 → FR-018 → FR-019 → FR-020
전체 이슈 20/20 완료 (#1~#20). 루프 1: #1·2·3·4·6·7·8·14 / 루프 2: #5·9·10·11·12·13·15·16·17·18·19·20
NEXT: 없음 — 첫 슬라이스 전 범위 구현·검증 완료. (남은 것은 사용자 확인 후 push/draft PR)
