# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 상태

CorpBrain — 100% 로컬 구동형 AI 지식 관리 솔루션. 현재 **그린필드**(소스 코드 없음). 만들 대상은 확정된 스펙 문서에 정의되어 있다: 코드보다 스펙이 먼저다.

## 스펙 주도 워크플로우 (필수)

기능 구현은 반드시 이 순서를 따른다:

1. 새 기능은 `/interview`로 요구사항을 결정 가능한 수준까지 확정한 뒤 `/spec`으로 스펙에 기록한다. 확정된 스펙 없이 새 기능 구현을 시작하지 않는다.
2. 구현은 확정된 스펙에 **엄격히 맞춘다** — 스펙에 없는 동작을 임의로 추가하지 않고, 비목표로 선언된 것을 슬쩍 넣지 않는다.
3. 구현이 끝나면 `/spec-check <스펙 경로>`로 스펙과 대조 검증한다.

## 스펙 문서 규칙

- 기능 스펙은 `static/docs/specs/features/<slug>.md`에 둔다 (이곳이 정본 위치).
- 스펙에는 이 대화에서 사용자가 결정했거나 승인한 것만 넣는다. 결정을 창작하지 않는다.
- 모든 항목에 출처 태그를 단다: `[사용자 결정]` 또는 `[제안 후 승인]`. 미결정은 "미결정 사항" 섹션으로 분리한다.

## 구현 스택 (첫 슬라이스)

첫 구현 대상과 계약은 @static/docs/specs/features/corpbrain-mvp-local-scan-to-wiki.md 에 정의되어 있다. 핵심 제약:

- 런타임은 **Python**. 코어/CLI 우선이며 UI(pywebview·React)는 이번 범위 밖.
- 외부 통신은 **로컬 Ollama 호출뿐** — 그 외 네트워크·텔레메트리 0.
- 두 구조 이음새를 유지한다: ① 비즈니스 로직은 재사용 가능한 **코어 라이브러리**에 두고 CLI는 얇은 어댑터, ② 모든 외부 네트워크 호출은 **단일 관문 함수**를 통과.

## v0.6 지식그래프 불변식

착수 중인 v0.6 슬라이스의 계약은 @static/docs/specs/features/corpbrain-v0.6-knowledge-graph.md 에 있다. 구현 시 유지할 것:

- 저장 계층(벡터 인덱스·그래프 DB)의 문서 키는 **원문 절대경로 문자열**로 통일한다. 상대경로 해석은 CLI 어댑터의 책임이며 코어로 넘기지 않는다.
- 그래프 DB는 **재료와 파생물을 분리**한다. 문서에서 추출한 사실(`doc_facts`)만 증분 upsert로 영속하고, 노드·엣지는 매 실행 전체 재빌드한다 — 임계치가 바뀌어도 옛 엣지가 남지 않게 한다.
- 그래프 빌더는 sqlite를 직접 열지 않는다. 벡터 접근은 `VectorStore` Protocol(`iter_vectors()`)을 통해서만 한다 — v0.4의 저장소 어댑터 이음새를 유지한다.
- 생성된 위키를 나중에 고쳐 쓸 때는 헤딩 문자열을 탐색해 자르지 않는다. `<!-- corpbrain:related:start/end -->` 같은 **기계 관리 마커 블록** 사이만 교체하고, 마커가 없으면 추가한다 — 요약 본문에 같은 헤딩 문자열이 들어간 문서에서 사용자 내용이 조용히 잘려 나가는 것을 막는다.
- 인덱싱을 건너뛰는 경로에서도 `if store is not None` 류의 분기를 새로 만들지 않는다. `_NoIndexStore` 같은 무동작 구현을 끼워 처리 경로를 하나로 유지한다.
- 생성물을 고쳐 쓸 때는 **내용이 실제로 달라졌을 때만 기록한다.** 같은 내용을 다시 써서 mtime만 갱신하지 않는다 — 동기화 도구가 매 실행마다 산출물 전체를 변경으로 인식한다.
- 임베딩 입력에는 **그래프가 만들어 낸 것(「관련 문서」 등)을 넣지 않는다.** 넣으면 유사도 → 관련 문서 → 임베딩 → 유사도의 피드백 루프가 생겨 같은 입력이 같은 결과를 내지 못한다.
- 조회 명령은 저장된 값을 그대로 읽는다. 조회 시점에 파생값을 다시 계산하지 않는다 — 출력이 DB·산출물과 어긋난다.
- 위키 본문에 렌더되지 않는 부가 필드는 요약 응답 **검증**에서 선택 필드로 둔다. 그 필드가 없다고 문서의 위키 생성 자체를 실패시키지 않으며, 이 검증 규칙은 엔진(local/cloud)별로 달리하지 않는다 — 같은 폴더가 엔진에 따라 다른 개수의 위키를 내면 안 된다.
- **요청** 스키마는 다르다. 스키마를 강제할 수 있는 엔진(클라우드 `tool_choice`)은 `required`에 넣어 확실히 받아 오고, 프롬프트로만 요청할 수 있는 엔진(로컬)은 그러지 않는다. 강제받는 쪽은 누락 자체가 불가능하므로 위키 개수는 여전히 갈리지 않는다. 선택으로 두면 강제받는 모델이 그 필드를 통째로 건너뛴다 — v0.6.0 클라우드 스모크에서 문서 6개 중 5개가 `entities: []`로 왔다. [2026-08-23 실측 · v0.6.1 정정]
- 저장소 스키마가 안 맞거나 파일이 손상되면 **자동 복구하지 않는다.** 에러로 멈추고 삭제·재생성을 안내한다 — 조용히 폐기하면 데이터 손실이 한참 뒤에야 드러난다.
- 그래프에서 **자기 자신을 향하는 엣지는 종류를 불문하고 만들지 않는다.** 판정은 유사도 같은 값이 아니라 `doc_id` 동일성으로 한다.
- 수동 스모크는 **스텁이 원리적으로 증명할 수 없는 것만** 확인한다(실제 모델의 응답 충실도, 실제 임베딩 분포에서의 임계치 타당성 등). 자동 테스트가 이미 덮는 것을 수동 절차로 복창하지 않는다.

## v0.7 하이브리드 검색 불변식

착수 예정인 v0.7 검색 슬라이스의 계약은 @static/docs/specs/features/corpbrain-v0.7-hybrid-search.md 에 있다. 구현 시 유지할 것:

- 그래프 확산은 **저장소 계약을 넓히지 않고** 성립시킨다. `GraphStore`(10멤버)도 `VectorStore`도 새 메서드를 더하지 않는다. 확산 문서의 코사인·제목·경로는 `search(query_vector, top_k=len(list_ids()))`로 **전 문서를 한 번에 받아** 매핑으로 들고 쓴다 — 기존 구현이 이미 전 행을 채점한 뒤 자르기만 하므로 추가 비용이 없고, 같은 문서가 시드로 나올 때와 확산으로 나올 때 제목이 갈리지 않는다.
- 검색 결과의 「왜 이 문서가 여기 있는가」는 **후보 진입 경로**로 가른다. 코사인 top-k로 들어온 시드는 근거 줄이 없고, 그래프 확산으로 처음 후보가 된 문서는 최종 점수가 자기 코사인으로 결정됐더라도 근거 줄을 갖는다 — 점수 출처로 가르면 top-k 밖인데 결과에 나타난 문서가 설명 없이 놓인다. 단, 같은 숫자를 한 줄에서 두 번 적지 않는다(대괄호 점수 == 코사인이면 「코사인」 항목 생략).
- 조회 명령(`search`)의 동점 tie-break는 **`doc_id`(원문 절대경로) 사전순**이다. 「출력 상대경로」를 쓰지 않는다 — 코어는 경로 해석을 하지 않고, 검색 시점엔 스캔 루트를 모르므로 그 값을 얻으려면 검색 1회마다 위키 전체를 열어야 한다. 파이프라인(`rank_related`)이 상대경로를 tie-break에 쓰는 것은 스캔 루트를 이미 알기 때문이며, 이 차이를 없애려고 조회 경로에 위키 I/O를 들이지 않는다.
- 순위 계산의 성질을 보장하는 상수(감쇠 계수 α 등)는 **코어가 유효 범위를 검증한다.** CLI 파서에 검증을 두지 않고, 조용히 클램프하지도 않는다 — 규칙이 한 곳에만 있어야 코어를 직접 부르는 후속 어댑터도 같은 보호를 받고, 스윕해 가며 효과를 재는 측정 절차가 «준 값과 다른 값으로 계산된 결과»를 보지 않는다.
- 실측으로 정하기로 한 기본 상수는 **잠정값임을 주석에 명시**하고, 자동 테스트는 그 상수를 참조하지 말고 값을 명시적으로 넘긴다. 실측 뒤 상수 한 줄만 교체하면 되게 하고, 잠정값인 채로 릴리스하지 않는다.

## v0.8 오피스 포맷 추출 불변식

착수 예정인 v0.8 슬라이스(`.xlsx`·`.xlsm`·`.pptx` 텍스트 추출)의 계약은
@static/docs/specs/features/corpbrain-v0.8-xlsx-pptx-extraction.md 에 있다. 구현 시 유지할 것:

- 시트·슬라이드 경계 줄(`[시트: …]`·`[슬라이드 N]`)은 **내용이 있을 때만** 낸다. 경계 줄은
  「뒤에 내용이 온다」는 신호이므로 뒤가 비면 낼 것이 없다. 항상 내면 값이 전부 비는 파일의
  추출 결과가 경계 줄만 남아 호출자의 `text.strip()` 검사를 통과하고, `empty_document` 로
  걸러져야 할 문서가 시트 이름만 든 입력으로 LLM 까지 간다. **빈 판정 책임은 호출자
  (`prepare_summary_input`) 한 곳에 그대로 둔다** — 추출기 밖으로 포맷 문법 지식을 흘려
  「경계 줄만 있으면 비었다」를 알게 하지 않는다.
- 추출 실패의 **원인 판정은 예외 메시지 문자열 매칭으로 하지 않는다.** 라이브러리 버전이 올라가면
  조용히 깨진다. 파일 시그니처처럼 안정된 근거를 쓰고, 근거가 두 원인을 구분하지 못하면 detail 에
  **두 원인을 함께 적어** 정직하게 둔다 (OLE 시그니처 = 「암호화되었거나 구형 이진 포맷」).
- 추출기는 `read_only` 개봉을 포기하지 않는다. 「파일을 통째로 메모리에 올리지 않는다」가
  숨김 행 판별 같은 부가 기능보다 우선하며, 라이브러리 제약으로 둘이 부딪치면 **부가 기능 쪽을
  포기하고 그 사실을 스펙에 기록한다.**

## v0.9 GUI 불변식

착수 예정인 v0.9 GUI 슬라이스의 계약은 @static/docs/specs/features/corpbrain-v0.9-gui.md 에
있다. **구현 순서·PR 절단·단계별 완료 판정은 @docs/plans/corpbrain-v0.9-gui.md 를 따른다** —
2 PR 수직 절단(인프라+대시보드 → 화면 5개), 코어 변경은 첫 호출자와 같은 커밋, DoD 12항목의
PR별 배분이 거기에 있다. 구현 시 유지할 것:

- 코어의 취소는 **순수 술어**(`should_cancel: Callable[[], bool]`)로 받는다. `threading.Event`
  같은 동시성 자료형을 코어 시그니처에 박지 않는다 — 코어에는 `threading`·`asyncio`·`signal`
  import가 하나도 없으며, 그 성질이 유지되어야 후속 어댑터가 프로세스·비동기로 가도 변환층이
  생기지 않는다. 이벤트 sink(`on_event`)의 반환값으로 제어를 표현하지도 않는다 — 표시 장치와
  실행 제어를 한 콜백에 섞으면 sink 버그가 스캔을 조용히 멈추는 실패 모드가 생긴다.
- **`on_event`의 예외는 삼키고 `should_cancel`의 예외는 삼키지 않는다.** sink는 표시 장치라
  죽어도 스캔이 계속되는 것이 옳지만, 취소 술어는 제어 입력이라 무시하면 사용자의 중단 요청이
  사라진다.
- **취소된 스캔은 그래프 단계(패스2·3)를 건너뛴다.** 안전한 이유는 `doc_facts` upsert가
  패스1의 파일 루프 안에서 문서마다 일어나기 때문이다 — 재료가 엔티티까지 남아 다음 `scan`이
  재요약 없이 그래프와 「관련 문서」를 자동 회복한다. 다만 그때까지의 창을 조용히 두지 않고
  「그래프 미반영」을 종료 보고에 낸다.
- **취소된 스캔은 고아 벡터 정리도 건너뛴다.** 그 정리는 파일 루프가 **방문한 문서만** 담은
  집합으로 「위키가 사라진 문서」를 판정하므로, 취소로 루프가 끊기면 아직 방문하지 않은 문서
  전부가 고아로 오판돼 벡터가 지워진다(문서 50개 중 3번째에서 취소 → 47건 삭제). 다음 실행이
  백필하므로 영구 손실은 아니나 문서마다 임베딩 호출을 다시 치르고 그 사실이 보고되지 않는다.
  v0.6이 「위키를 하나라도 읽지 못하면 재료 정리를 통째로 건너뛴다」로 세운 잣대와 같다 —
  **목록이 불완전한 채로 «없는 문서»를 판정하지 않는다.**
- **취소 여부는 `ScanResult.cancelled`(최상위 필드)로 알린다.** `GraphOutcome`·
  `GraphSkipReason` 안에 넣지 않는다 — 「스캔이 중단됐다」는 실행 전체의 상태여서, 그래프
  하위 객체에 숨기면 그래프를 보지 않는 호출자가 취소를 알 수 없다. v0.6의 「`ScanResult`
  9필드 유지」는 «그래프 지표를 평면 필드로 흩뿌리지 않는다»는 뜻이므로 그래프 지표가 아닌
  값에는 적용되지 않으며, 그래프 관련 값은 종전대로 `GraphOutcome` 안에서만 늘어난다.
- 그래프 단계 진행 이벤트는 **`GraphStarted` · `RelatedInjected(index, total, path)` ·
  `GraphFinished(stats)` 3종**이며, **진행률은 쪼갤 수 있는 패스3에만 싣는다** — 없는 진행률을
  지어내지 않는다. 기존 `FileStage`의 `Stage` enum에 값을 더해 재사용하지 않는다: 그 타입은
  「문서 1개를 처리하는 중」을 뜻하므로 의미가 달라지고 `reduce()`의 `generated` 카운트와
  rate·ETA 계산이 오염된다.
- SSE는 **접속 즉시 현재 `StatusSnapshot`을 한 번 보내고** 그 뒤 실시간 이벤트를 잇는다.
  이벤트를 버퍼링해 리플레이하지 않는다 — `EventSource`는 끊기면 자동 재연결하므로 재접속은
  상시 사건이고, 버퍼 방식은 스캔 1회분 이벤트를 계속 쥐면서 상한·생애 정책이라는 결정을
  새로 만든다. 서버는 이미 `reduce()`로 접어 둔 스냅샷을 들고 있으므로 추가 상태가 없다.
- GUI 인증은 **부트스트랩 쿼리 토큰 → `HttpOnly`·`SameSite=Strict` 세션 쿠키 교환** 하나로
  통일한다. `Authorization` 헤더 방식을 쓰지 않는 이유는 브라우저 `EventSource`가 커스텀
  헤더를 붙일 수 없어 SSE 하나에서 경로가 갈리기 때문이다. 인증 경로를 둘로 나누지 않는다.
  쿠키와 `Origin`/`Host` 검증은 **서로를 대체하지 않으며 둘 다 필요하다** — 쿠키는 같은 머신의
  다른 프로세스를, Origin 검증은 브라우저발 CSRF·DNS rebinding을 각각 가른다.
- **`Host`와 `Origin`은 「없을 때」의 판정이 다르다.** `Host`는 항상 필수이며
  `127.0.0.1:<port>`와 정확히 일치해야 한다(DNS rebinding). `Origin`은 **상태를 바꾸는
  메서드에서만 필수**이고, 조회 GET에서는 **부재를 허용한다** — 브라우저가 최상위 GET
  내비게이션에 `Origin`을 붙이지 않으므로 「없음 = 403」으로 구현하면 기동 시 여는 첫 화면이
  서버 자신에게 막힌다.
- 전체 그래프 조회를 위해 `GraphStore` 계약을 **`iter_edges()` 하나만** 넓혀 11멤버로 둔다
  (노드는 `degree_ranking()` + `nodes_of()`로 기존 계약에서 얻어진다). 기존 10멤버로 짜내지
  않는다 — 전 노드에 `neighbors()`를 돌리면 N+1 쿼리와 **대칭 엣지 중복 제거 로직이 조회
  어댑터에 생겨**, 「대칭 엣지는 한 행으로 저장한다」는 저장 규칙 지식이 저장소 밖으로 샌다.
  v0.7이 계약을 지킬 수 있었던 것은 `search(top_k=전 문서)`처럼 **이미 있는 메서드가 전 행을
  돌려주는 길**이 있었기 때문이며, 엣지에는 그 길이 없다. 새 테이블·새 저장 계층은 만들지
  않는다 — `iter_edges()`는 `replace_graph()`의 읽기 대칭항일 뿐이다.
- GUI 서버에서 **HTTP 상태코드는 프로토콜 층 사건에만** 쓴다(401·403·409·404·405). 코어 예외는
  **열거가 아니라 기반 클래스로** 가른다 — **`CorpBrainError`면 도메인(200 + 구조화된 상태 본문),
  아니면 버그(500)**. 열거하면 목록에 없는 `GatewayError`·`ExtractionError` 등이 새는 순간
  버그가 아닌데 500이 되어 「로그의 500 = 버그 신호」가 무너진다. 본문에는 예외 클래스명과 안내
  문장을 각각 필드로 담아 프론트가 문자열을 파싱하지 않게 한다. 이들은 요청의 잘못도 서버의
  잘못도 아닌 **환경의 상태**이고, 4xx로 접으면 서로 다른 안내가 한 코드에 뭉친다. CLI
  종료코드(1·3)를 응답에 싣지 않는다 — 종료코드는 프로세스가 죽는 방식이지 서버 응답의 어휘가
  아니다.
- **`sqlite3.Error`는 도메인이되 `sqlite3.ProgrammingError`는 500이다.** 코어의 커넥션은
  `check_same_thread` 기본값으로 열리고 `ThreadingHTTPServer`는 요청마다 스레드가 다르므로,
  그 예외는 손상이 아니라 **스레드 오용 버그**다. 200 + 「DB를 지우고 다시 스캔하세요」로 내보내면
  사용자가 멀쩡한 DB를 지운다. 조회 엔드포인트는 **요청마다 저장소를 열고 `finally`에서 닫으며**
  커넥션을 요청 사이에 캐시하지 않는다.
- GUI의 스캔은 **계량 → 확인 → 실행** 2단계이며, 계량 결과(`plan`·`findings`)의 재사용은
  **계량에 쓰인 `ScanConfig`와 완전히 같을 때로 한정**한다. 다르면 조용히 다시 계량한다 —
  `ScanPlan.gate`는 계량 시점 임계값으로 이미 내려진 판정이고 코어의 방어는 파일 수 비교
  하나뿐이라, 한도를 올려도 낡은 판정에 계속 막힌다. CLI는 한 프로세스 안에서 연달아 돌아
  이 문제가 없었고 두 요청으로 쪼갠 GUI가 새로 만드는 요구다.
- GUI는 `ScanConfig` 15필드와 `search_index` 파라미터를 **전부 다룰 수 있게 하되** 앞면과
  「고급」 접기로 나눈다. **실측으로 확정된 상수**(`similarity_threshold` = 0.5717… ·
  `graph_decay` = 0.5)는 앞면에 두지 않는다 — 앞면에 있으면 근거 없이 만져지고, 만지면
  실측으로 세운 성질이 조용히 무너진다. 「CLI로 돌아가야만 되는 일」을 남기지 않는 것과
  「근거 있는 기본값을 지키는 것」을 접기 하나로 동시에 만족시킨다.
- GUI는 파라미터 **검증을 자체적으로 두지 않는다.** 값을 그대로 코어에 넘긴다 —
  `validate_graph_decay()`·`parse_expand_edges()`가 이미 코어에 있고, v0.7 §4.4가 「규칙이 한
  곳에만 있어야 코어를 직접 부르는 후속 어댑터도 같은 보호를 받는다」고 정한 그 후속 어댑터가
  이 GUI다.
- `~/.corpbrain/config.json` 쓰기는 **직전 재읽기 → 자기 섹션만 교체 → 임시파일 후 `rename`**
  순으로 한다(`gui`·`cloud_consent` 양쪽). 두 어댑터가 한 파일을 쓰므로 「읽기 → 수정 → 통째로
  쓰기」는 나중에 쓴 쪽이 상대 섹션을 날린다 — 동의가 조용히 철회되면 클라우드 스캔이 막히고
  원인이 드러나지 않는다. 파일 락은 두지 않는다: POSIX/Windows 분기를 코어에 들이는 대가가
  설정 저장 빈도에 비해 과하고, 현재 코어의 플랫폼 분기는 파일 숨김 처리 한 곳뿐이다.
- GUI 서버의 요청 처리는 **소켓을 모르는 순수 함수**(`handle(method, path, headers, body)
  -> Response`)로 두고 `http.server` 핸들러는 그것을 부르기만 한다. 인증·Origin 검증·라우팅·
  직렬화·상태코드가 전부 그 함수 안에 있어야 테스트가 소켓 없이 결정적으로 단언한다 —
  `report.py`의 순수 빌더가 표시 로직을 소유하는 것과 같은 관용구다. 실제 소켓을 여는 테스트는
  「뜨고 붙는다」 확인에 한정한다.
- 「듣는 소켓은 127.0.0.1뿐」은 **`SocketWatcher`가 `bind`를 기록한 뒤 원래 `bind`에 넘기는
  방식**(pass-through)으로 단언한다. 기존 watcher는 `connect`·`create_connection`만 패치해
  나가는 연결만 보므로 이 성질을 측정할 수 없다. **기록만 하고 가로채면 안 된다** —
  `listen()`이 바인드되지 않은 소켓을 `0.0.0.0`의 임의 포트로 자동 바인드해, 그 성질을
  증명하려는 테스트가 오히려 전 인터페이스에 소켓을 연다. `bind`는 `connect`가 아니라 fixture의
  연결 차단과 애초에 부딪치지 않으므로 가로챌 이유도 없다. 상수(`HOST`)만 단언하는 방식은 쓰지
  않는다 — 상수는 맞는데 실제 `bind` 호출이 다른
  값을 쓰면 감시장치가 공허하게 통과한다. 케이스는 `tests/security/test_network_invariant.py`
  안에 둔다(새 파일로 분리하지 않는다).
- `watch_sockets` fixture에 **「허용 목록」 예외를 만들지 않는다.** `_fake_connect`가 연결을
  거부하는 것은 결함이 아니라 「감시를 우회한 연결은 실패해야 한다」는 의도이며, 실제 연결이
  필요한 테스트(GUI 기동 스모크 등)는 이 fixture를 쓰지 않는 쪽으로 푼다. 예외는 하나 생기면
  늘어나고, 그때마다 불변식 테스트가 스스로 약해지는데 그걸 막는 것은 코드리뷰뿐이다.
- SSE는 **「이벤트 시퀀스」와 「프레임 직렬화」를 나눠 각각 순수하게 검증한다.** 끝나지 않는
  스트림을 테스트에서 통과시키지 않는다 — 실제 서버를 띄워 읽고 타임아웃으로 끊는 방식은
  타임아웃 상수가 들어와 CI에서 가장 자주 깨진다. `Response.body`를 이터레이터까지 받도록
  넓히지도 않는다: 타입이 두 종류가 되어 모든 호출부가 둘을 가려야 한다.
- 「진행 중」이 전제인 테스트(409 · 취소)는 **상태를 주입한 순수 판정**으로 푼다. `sleep`·
  `threading.Event`·`Barrier`를 테스트에 들이지 않는다 — 타이밍 의존 테스트는 CI 부하에 따라
  간헐 실패하고 깨질 때마다 대기 시간이 늘며, 동기화 장치는 데드락 가능성과 함께 코어에 없는
  `threading`을 테스트가 들여오게 한다. 취소는 `should_cancel`이 순수 술어이므로 「N번째 문서
  뒤에 True」로 정확한 지점이 재현된다.
- GUI 화면 상태는 **URL 해시가 단일 출처**다(`#/graph?node=…`). 화면 전환은 해시를 바꾸는
  것으로만 일어나고 뷰 전환 함수를 직접 부르지 않는다 — 그래야 해시와 화면이 어긋나지 않는다.
  `pushState`를 쓰지 않는 이유는 서버가 모르는 경로 전부에서 index를 내주는 SPA fallback이
  필요해져 404 처리 규칙과 섞이기 때문이다. 해시는 서버로 전송되지 않아 서버 라우팅을 건드리지
  않는다.
- GUI는 **파일을 OS 기본 앱으로 여는 엔드포인트를 두지 않는다.** 위키의 `file://` 「원문」
  링크는 http 페이지에서 브라우저가 차단하므로 죽은 링크 대신 **경로 표시 + 복사 버튼**으로
  낸다. `os.startfile`/`open`/`xdg-open` 브릿지는 MVP 스펙 §2의 명시적 비목표이며, 되살리면
  「서버가 임의 파일을 OS 기본 앱으로 실행한다」는 새 능력이 생긴다 — 토큰 인증이 있어도 이는
  별개의 결정이다. 위키 산출물(`render.py`의 `file://` 링크) 자체는 바꾸지 않는다: 파일
  탐색기·에디터에서 볼 때는 여전히 동작한다.
- 위키 상세 응답의 「관련 문서」는 **`doc_id`를 함께 실어** 내린다. 프론트엔드가 마크다운
  링크의 상대경로를 파싱해 되돌리게 하지 않는다 — 경로 해석이 프론트로 넘어가고, 「프론트엔드에
  마크다운 파서를 두지 않는다」와도 부딪친다. 다만 **위키 본문에 `doc_id`는 적혀 있지 않으므로**
  서버가 상대 링크를 그 위키 파일 기준으로 풀어 대상 위키의 front-matter `source_path`를
  읽는다(`rerun.read_source_path()`, 상세 1건당 최대 `related_top_k`개 파일 열기). 그래프에서
  `rank_related()`로 다시 계산하지 않는다 — 동점 정렬 키가 달라져 화면 순서가 파일에 적힌
  순서와 어긋난다.
- 위키 상세는 **front-matter 5키**(`source_path`·`generated_at`·`model`·`engine`·`source_bytes`)를
  낸다. `engine`을 빼지 않는다 — 「이 문서가 외부로 나갔는가」를 생성물만 보고 아는 값이다.
  파싱은 `embedding_text`에 **`parse_wiki_document()`를 추가**해 하고, 기존
  `parse_wiki_markdown()`은 3-튜플 반환을 유지한 얇은 래퍼로 남긴다 — 그 함수는 front-matter를
  버리고 섹션을 합본 텍스트로 뭉개므로 상세 화면을 만들 수 없고, 반환을 바꾸면 기존 호출부
  세 곳이 함께 깨진다.
- GUI가 `report.py` 빌더를 재사용할 때 **갈라지면 안 되는 것은 「어휘」이지 「줄 조립」이
  아니다.** 검색 결과는 `doc_id`·`score`·`title`·`source_path`를 필드로 내려 카드가 그리되,
  확산 문서의 **근거 줄만** 기존 빌더의 결과 문자열을 그대로 싣는다 — v0.7이 정확 문자열까지
  못박고 단위테스트가 단언하는 계약이기 때문이다. 줄 문자열 전체를 내려 프론트가 정규식으로
  다시 가르게 하지 않는다(출력 문구를 다듬는 순간 조용히 깨진다).
- GUI 프론트엔드는 **계승한 디자인 토큰 밖의 색을 만들지 않는다.** minimalist의
  `--text-secondary`·`--text-muted`는 대비비 실패에서 한 번 재서 올린 값이고(이력이
  `style.css` 주석에 있다), 토큰을 벗어나면 그 비용이 무효가 된다. 클릭 가능한 요소는 전부
  키보드로 닿고 포커스 링이 보여야 한다. 자동 접근성 검사는 도입하지 않는다 — Node 툴체인과
  CI 브라우저 구동이 다시 들어오며, 이는 수동 스모크 항목으로 확인한다.
