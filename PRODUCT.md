# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**주 사용자는 「문서를 가진 실무자」이며 개발자가 아니다.** [사용자 확인 · 2026-08-28]

- **상황**: 회사 문서가 공유드라이브·메신저·개인 노트북에 흩어져 있다. 내용으로 찾고 싶지만
  파일명을 이미 알고 있어야만 찾을 수 있다.
- **하려는 일(job)**: ① 내용으로 문서를 찾는다 ② 문서가 무엇에 관한 것인지 열어 보지 않고 안다
  ③ 관련된 다른 문서로 건너간다.
- **결정적 제약**: 대상 문서가 인사·계약·원가처럼 **외부 서비스에 올릴 수 없는 것**이다. 이 제약이
  없으면 이 제품을 쓸 이유가 없다.

**확인된 격차 — 사용자와 현재 설치 경로가 어긋나 있다.** 주 사용자는 비개발자인데 현재 설치는
`uv sync` + `ollama pull` ×2(모델 7.2GB) + 터미널 명령을 요구한다. 이는 알려진 결함이며 설계
의도가 아니다. 온보딩·설치·첫 실행 경험은 부가 기능이 아니라 **이 사용자에게는 제품 그 자체**다.

## Product Purpose

흩어진 로컬 문서를 스캔·요약해 **마크다운 위키 · 벡터 검색 · 지식그래프**로 만든다. 클라우드
요약은 옵트인이다. (`pyproject.toml` description)

존재 이유는 하나다 — **외부에 올릴 수 없는 문서에도 같은 일을 해 주는 것.**

**성공의 정의는 아직 정해지지 않았다.** [사용자 확인 · 2026-08-28 · 미결정]
2026-08-28 사내 발표에서 도입 방향(①로컬 설치형 · ②온프레미스 · ③클라우드 서비스)에 대한
의견을 수렴한 뒤 정한다. 후보 세 가지는 아래와 같으며, **어느 하나를 성공 기준인 것처럼 다루지
않는다.**

1. 사내 파일럿에서 한 부서가 실제로 계속 쓰게 되는 것
2. 사외 배포 / 오픈소스 공개
3. 개발 방법론 시연이 주목적이고 제품은 그 증거물

## Positioning

**세 가지가 함께 있을 때만 성립한다.** [사용자 확인 · 2026-08-28] 하나씩 떼면 이웃 제품이
저마다 따라 할 수 있고, 셋이 같이 있는 제품이 없다는 것이 이 제품의 자리다.

1. **문서가 기기를 밖으로 나가지 않는다.** 기본 동작에서 외부 통신은 0이고, 듣는 소켓은
   `127.0.0.1` 뿐이다. 이것은 문서상의 약속이 아니라 **테스트가 `bind`와 `connect`를 모두
   감시해 기계적으로 보장**한다(`tests/security/test_network_invariant.py`).
2. **인프라가 아니라 완제품이다.** 신규 런타임 의존성 0으로 브라우저 화면 6개가 뜬다. 그래프
   DB도, 벡터 스토어 백엔드도, 컨테이너 오케스트레이션도 필요 없다. 사용자가 그 위에 무언가를
   만들지 않아도 된다.
3. **산출물이 평범한 마크다운 파일이다.** 도구를 버려도 위키가 남는다. 결과가 제품 내부 저장소에
   갇히지 않으므로, 다른 도구로 갈아탈 때 잃는 것이 적다.

## Operating Context

- **실행**: `corpbrain gui --out <dir>` → 로컬 서버가 `127.0.0.1` 임의 포트에 뜨고 토큰이 실린
  URL을 stdout에 낸 뒤 브라우저를 연다. 포그라운드로 떠 있고 `Ctrl+C`로 끝난다.
- **명령 7종**: `scan` · `plan`(계량, LLM·네트워크 0) · `doctor`(환경 점검) · `search` ·
  `graph` · `gui` · `consent`.
- **화면 6개**: 대시보드 · 플랜 & 스캔 · 지식그래프 · 위키 탐색기 · 지식 검색 · 설정.
  화면 상태의 단일 출처는 URL 해시(`#/graph?node=…`)다.
- **필수 사전 준비**: 로컬 Ollama 구동 + 모델 2종 — 요약 `qwen2.5:7b-instruct`(4.7GB),
  임베딩 `qwen3-embedding:4b`(2.5GB). 합계 7.2GB. 임베딩 모델이 없으면 `scan`이 exit 1로 막힌다.
- **하드웨어**: GPU가 감지되지 않으면 자원 게이트로 exit 3(`--force-gates`로 강행 가능).
  CPU 실측 문서 1건당 52~62초. **클릭하고 기다리는 도구가 아니라 걸어 두는 도구**다.
- **산출물 위치**: `<out>/<원본이름.확장자>.md`(입력 폴더 구조 미러링) ·
  `.corpbrain_index.sqlite`(벡터) · `.corpbrain_graph.sqlite`(그래프).
- **설정 파일**: `~/.corpbrain/config.json` — `gui`와 `cloud_consent` 두 섹션을 두 어댑터가 공유한다.

## Capabilities and Constraints

**할 수 있는 것**
- 지원 포맷 7종: `.docx` · `.pdf` · `.xlsx` · `.xlsm` · `.pptx` · `.txt` · `.md`.
- 문서 1개 → 위키 1개. 원문 mtime 기준 증분 재생성(`--force`로 강제).
- 지식그래프 — 노드 3종(Document·Entity·Tag), 엣지 4종(태그 소속·엔티티 포함·시맨틱 유사도·명시적 참조).
- 하이브리드 검색 — 코사인 top-k 시드 + 그래프 1홉 확산.
- 클라우드 엔진(Anthropic) **옵트인** — 명시 동의를 1회 기록해야 켜지고, PII 7종이 자동
  마스킹되며, 생성물 front-matter의 `engine` 값으로 「이 문서가 외부로 나갔는가」가 남는다.
  API 키는 `ANTHROPIC_API_KEY` 환경변수로만 읽고 **파일에 저장하지 않는다.**

**의도적으로 하지 않는 것** (결함이 아니라 결정)
- 멀티유저 · 인증 계정 · 동시 접속자 관리 · 외부 바인드(`--host` 없음).
- 여러 워크스페이스 — 작업 공간은 `--out` 하나다.
- 동시 스캔 — 한 번에 하나이며 두 번째 요청은 409로 거절된다.
- 파일을 OS 기본 앱으로 여는 브릿지 — 「서버가 임의 파일을 실행한다」는 별개의 능력이다.
- OCR · 스캔 이미지 PDF · 구형 `.xls`/`.ppt`.
- 저장소 손상 시 자동 복구 — 멈추고 삭제·재생성을 안내한다.

**용어** (화면과 문서에서 한 어휘로 유지한다)
위키 · 지식그래프 · 관련 문서 · 시드 · 확산 · 게이트 · 계량(`plan`) · 스모크 · 옵트인.

**미결정 사항** (지어내지 말 것)
- **사업화 방향** ①로컬 간편 설치형 / ②온프레미스·중앙 워크스테이션 / ③클라우드 서비스 —
  2026-08-28 발표의 의견 수렴 결과로 정한다.
- **라이선스** — `LICENSE` 파일이 없다. PyPI 배포와 함께 v0.9 비목표로 미뤄져 있다.
- 스캔 규모 상한 기본값 50(`--max`)을 넘는 사용 시나리오.

## Brand Commitments

- **이름은 CorpBrain**이다. 패키지명·CLI 명령·문서 전부 이 이름을 쓴다.
- **언어는 한국어 단일**이다. UI·문서뿐 아니라 **LLM 요약 출력 언어도 항상 한국어**로 고정돼
  있다(MVP 스펙 §4.3 [사용자 결정]). 영문 병행 계획은 없다.
- 저작권 표기: `Copyright © 2026 CorpBrain Team. All rights reserved.` (README)
- **로고·아이콘·이미지 에셋이 하나도 없다.** 저장소에 `.svg`·`.png` 0개다. 없는 에셋을
  있는 것처럼 참조하지 않는다.
- 시각적 정본은 이 파일이 소유하지 않는다. 다만 프로젝트 규칙(`CLAUDE.md`)이
  **「계승한 디자인 토큰 밖의 색을 만들지 않는다」**를 불변식으로 두고 있으며, 그 근거는 두
  텍스트 색이 대비비 실패에서 한 번 재서 올린 값이라는 것이다. 이 제약은 구속력이 있다.

## Evidence on Hand

**있는 것 — 실측 근거**
- `docs/SMOKE.md` — 실제 모델로 돌린 수동 스모크 11회(실행 A~K)의 절차와 결과.
- 임베딩 모델 5종 실측 비교(문서 24개): top-1 적중률 27% → 82%로 기본 모델 교체.
  원시 데이터 `docs/smoke/embedding_reassessment_results.{json,csv}`.
- 그래프 확산 계수 α 스윕 원시 데이터 `docs/smoke/graph_decay_results.{json,csv}` +
  라벨링된 쿼리 세트 `graph_decay_queries.json`.
- 한국어 스모크 코퍼스 `docs/smoke/corpus/` — 문서 24개, 하위 폴더 6개.
- 확정 스펙 11종 `static/docs/specs/features/` · 결정 원장 17종 `docs/grill/`.
- 자동 테스트 1,051개. 네트워크 불변식 테스트가 `bind`·`connect`를 모두 감시한다.
- 리포지토리 **밖**에 비즈니스 분석 산출물 168개 파일(`~/modu2026/CorpBrain-plan-02/`) —
  Five Forces · 경쟁사 · 가치사슬 · KSF · TAM/SAM/SOM · 페르소나 · 여정지도 · JTBD · VPS · PRD · SRS.
  **이 저장소의 정본이 아니며 참고 자료다** (MVP 스펙 §2가 PRD를 스펙의 근거로 삼지 않기로 결정).

**없는 것 — 만들어 내지 말 것**
- 실사용자 인터뷰·사용 로그·정착률 데이터가 없다. 파일럿을 아직 하지 않았다.
- 실제 고객·도입 사례·추천사가 없다.
- 가격·매출·계약 조건이 없다.
- 경쟁 제품 대비 성능 벤치마크가 없다. 비교는 기능·구조 수준에서만 근거가 있다.
- 로고·브랜드 이미지·스크린샷 에셋이 없다.

## Product Principles

1. **기본은 로컬이고 외부는 옵트인이다.** 기본값에서 나가는 통신은 0이며, 나가야 할 때는
   명시 동의 → PII 마스킹 → 단일 관문을 반드시 지난다. 이 순서를 우회하는 경로를 만들지 않는다.
2. **산출물은 도구보다 오래 남는다.** 결과를 제품 내부 저장소에 가두지 않는다. 사용자가 이
   제품을 버려도 손에 남는 것이 있어야 한다.
3. **모르는 상태에서는 판정하지 않는다.** 저장소가 손상됐거나 목록이 불완전하거나 실행이
   중단됐을 때, 조용히 복구하거나 «없는 것»으로 단정하지 않는다. 멈추고 사람에게 알린다.
   조용한 복구는 데이터 손실을 한참 뒤에 드러나게 한다.
4. **순위와 임계치를 좌우하는 값은 실측으로 정한다.** 근거 없는 기본값을 릴리스하지 않는다.
   실측이 기존 결정을 뒤집으면 결정을 고치고, 왜 틀렸는지까지 기록한다.
5. **규칙은 한 곳에만 둔다.** CLI와 GUI는 같은 코어를 부르는 동급의 얇은 어댑터이며, 검증·
   경로 해석·표시 어휘를 어댑터가 따로 갖지 않는다. 그래야 두 화면이 같은 것을 다르게 말하지 않는다.

## Accessibility & Inclusion

- 본문 대비비 **AA(4.5:1) 이상**을 유지한다. 계승한 텍스트 색 토큰은 대비비 실패에서 한 번
  올려 잡은 값이므로 임의로 낮추지 않는다.
- **클릭 가능한 요소는 전부 키보드로 닿고 포커스 링이 보인다.** 그래프 캔버스를 포함한다.
- **자동 접근성 검사(axe-core 등)를 도입하지 않기로 결정했다** — Node 툴체인과 CI 브라우저
  구동을 들이지 않기 위해서다. 대신 릴리스 전 **수동 스모크 항목으로 사람이 확인한다**.
  화면을 손대면 같은 절차를 다시 밟는다. 자동 게이트가 대신 지켜 주지 않는다.
- UI 언어는 한국어 단일이며 i18n 계획이 없다.
