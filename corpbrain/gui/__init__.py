"""로컬 GUI 서버 — CLI와 동급의 얇은 어댑터 (v0.9 스펙 §4.10).

코어에 비즈니스 로직을 더하지 않고 코어 호출·JSON 직렬화·경로 해석·상태코드 매핑만 한다.
코어는 GUI의 존재를 알지 못한다 (MVP 스펙 §4.5의 이음새를 그대로 계승).

신규 런타임 의존성은 0이다 — 서버는 표준 라이브러리 `http.server`, 프론트는 빌드 스텝 없는
바닐라 JS/CSS 다.
"""
